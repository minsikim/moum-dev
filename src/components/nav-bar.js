import React from 'react';

const navbar = (props) => {
    const style= {
        width: '60px',
        backgroundColor: '#333',
        height: '100%'
    }
    return (
        <div style={style}>
        </div>
    );
}

export default navbar;